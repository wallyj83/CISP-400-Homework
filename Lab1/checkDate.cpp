#include "date_type.h"
using namespace std;

bool checkDate(DATE_TYPE&);


bool checkDate(DATE_TYPE &user_date) {

	// check a DATE_TYPE for validity, store status code in the DATE_TYPE

// return TRUE if all well, FALSE if error
}

